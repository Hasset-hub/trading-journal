// === Supabase configuration ===
// Paste your project's values below (Supabase dashboard → Settings → API).
//
//   url     = "Project URL"        e.g. https://abcdefghijkl.supabase.co
//   anonKey = "anon public" key    (safe to ship in client code — it is designed
//                                   to be public and your data is protected by
//                                   Row Level Security)
//
// NEVER put the "service_role" key here. That key bypasses all security rules.
//
// Leave these empty to run the journal in local-only mode (no accounts, data
// stays in this browser exactly as before).

window.SUPABASE_CONFIG = {
  url: '',
  anonKey: '',
};
